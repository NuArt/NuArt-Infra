# Ověření proti realitě serveru — 2026-06-30

Provedeno na Docker VM (192.168.0.199, hostname `docker`) jako součást stavby repa.
Pouze čtení (`docker ps`, `cat`, `psql \l`, `getent`, `curl`, `ping`). Nic se neměnilo.

## Co sedělo (dump = realita)
| Fakt | Ověřeno |
|---|---|
| Docker 29.6.0, Compose v5.1.4 | ✅ |
| Docker VM RAM ~20 GB | ✅ (`free -h` → 19Gi) |
| nuart-backoffice interní 3000 / host 3020 | ✅ |
| nicotrans-crm interní 3000 / bez host mappingu | ✅ |
| nicotrans-palety interní 3000 / host 3010 | ✅ |
| infra-postgres 5432, infra-minio 9000/9001 | ✅ |
| caddy 80/443, dockge 5001 | ✅ |
| windrose network_mode host, cpus 2.0, mem_limit 10g | ✅ |
| office.nuart.cz → nuart-backoffice:3000 (Caddy) | ✅ |
| Veřejná IP 178.255.174.235 (office/crm rezolvují) | ✅ |
| Ollama @ 192.168.0.154, modely qwen3:8b + qwen3:4b | ✅ (.153 už mrtvá) |
| DB crm_identity existuje | ✅ |

## Co se lišilo (bylo → je, opraveno v repu)
| # | Bylo (dump) | Je (realita) | Soubor |
|---|---|---|---|
| 1 | openwebui interní port 3000 | interní **8080**, host **3000** (3000→8080) | stacks/openwebui.md, docker-host/porty.md |
| 2 | crm.nuart.cz → jen nicotrans-crm (root) | crm.nuart.cz navíc `/palety/*` → nicotrans-palety (path-based) | stacks/caddy.md |
| 3 | palety „interní, bez auth, nevystaveno" | **vystaveno** pod crm.nuart.cz/palety, gate přes CRM identitu (IDENTITY_DATABASE_URL, CORE_URL) | stacks/nicotrans-palety.md |
| 4 | DB uživatelé: postgres, crm_user | + dedikovaní **nuart_user, nicotrans_user** | docker-host/infra/postgres.md |
| 5 | backoffice „nepushnutý commit?" | commit trustedOrigins JE pushnutý; netrackovaný jen docs/BETTER_AUTH_PROXY.md | stacks/nuart-backoffice.md |
| 6 | (neuvedeno) | navíc kontejner `valheim` (Exited, odstavený) + `buildx_buildkit_nicotrans-builder0` | docker-host/prehled.md |
| 7 | dockge mezi stacky v /opt/stacks/ | dockge běží z **/opt/dockge** | stacks/dockge.md |
| 8 | infra-minio host mapping „?" | mapováno 9000-9001 na host | docker-host/infra/minio.md |
| 9 | nico-palety.nuart.cz jen plán | A-záznam **už existuje** (→ 178.255.174.235), Caddy ho neobsluhuje | proxmox/sit.md |

## Co zůstává [OVĚŘIT] a proč
| Téma | Důvod, proč neověřeno |
|---|---|
| Celá Proxmox úroveň (pveversion, qm/pct config, zdroje VM/LXC) | **Z Docker VM není SSH na `pve`** (`root@192.168.0.186` → Permission denied, publickey/password). Nutno ověřit přímo na pve. |
| Hardware RAM (32 vs 64 GB), GPU passthrough detail | Vyžaduje přístup na pve (dmidecode/lspci) |
| Ollama statická IP (LXC 101) | Nelze ověřit z hostu, jen že .154 aktuálně žije (DHCP riziko trvá) |
| Uptime Kuma — co monitoruje | Neověřováno (LXC 102) |
| Proxmox backup joby, DB zálohy | Pravděpodobná díra; vyžaduje pve / cron audit |
| CRM finální login + změna admin hesla | Vyžadovalo by přihlášení do aplikace |
| Postgres heslo rotace | Provozní TODO (heslo uniklo do chatu) |
| windrose_plus.json (RCON cesta), Windrose+ verze | Soubor na dokumentované cestě 2026-06-30 nenalezen |
| Double-NAT / OpenVPN, subnet renumbering 10.44.10.0/24 | Síťové TODO, neměnitelné z hostu |

## 2. průchod (manuálně s Danielem, 2026-06-30) — všechny [OVĚŘIT] dořešeny ✅
Daniel pustil čtecí příkazy na `pve` a v Kumě; zbytek doověřen z Docker VM.

| Téma | Výsledek |
|---|---|
| Proxmox verze | `pve-manager/9.2.3`, kernel 6.17.13-13-pve ✅ |
| VM/LXC zdroje | VM100 4c/20GB/300G; LXC101 4c/8GB; LXC102 1c/1GB ✅ |
| Hardware RAM | **stále 32 GB** (31.2 GiB) — upgrade na 64 NEproběhl |
| GPU passthrough | potvrzen (dev0–3: nvidia0/nvidiactl/nvidia-uvm/-tools, gid=44) |
| Ollama IP | **stále DHCP** (MAC BC:24:11:46:DE:91) → rozhodnuto: DHCP rezervace na UniFi (TODO) |
| Uptime Kuma | 4 monitory (Docker VM, Proxmox, Backoffice, Palety); díry: CRM, veřejné domény/TLS, Ollama/PG/MinIO/Windrose nehlídány |
| Zálohy | **off-site na Wedos běží denně** (dump nevěděl!); díry: `crm_identity` + `Caddyfile` se nezálohují; Proxmox **vzdump neběží** |
| CRM login/heslo | login funguje (3 sessions); admin **stále na seed hesle** (TODO změnit) |
| Postgres rotace | vědomě **NEřešit** (nízké riziko, interní) |
| Windrose+ | v1.3.14; `windrose_plus.json` je v `server-files/` (ne ve `…_data/`) |
| Double-NAT | **funguje** (není blokátor) |
| VPN problém | byl konflikt IP rozsahu (ne double-NAT) |
| Subnet renumbering | 10.44.10.0/24 **stále v plánu** (kvůli VPN konfliktu) |
| ADR 001–010 | 8/10 platí beze změny; ADR-006 přeformulováno (moduly pod CRM = path-based), ADR-007 potvrzeno (gate přes CRM identitu) |

### Otevřené akční TODO (po ověření)
1. DHCP rezervace Ollama na UniFi (.154 napevno)
2. Rozšířit `/opt/nuart-backup.sh`: přidat `crm_identity` + `Caddyfile` (priorita 1; vejde se do 5 GB Wedos limitu)
3. Změnit CRM admin heslo (ze seedu)
4. Zvážit doplnění Kuma monitorů (CRM, veřejné domény přes Caddy = TLS)
5. Subnet renumbering 10.44.10.0/24 (až bude vhodný čas)
6. Zvážit Proxmox vzdump job (pozor na místo na `/`)

(RAM upgrade 32→64 GB byl jen dotaz, NE plán — vyřazeno.)
(Bod „rozšířit zálohu o crm_identity + Caddyfile" ✅ HOTOVO 2026-06-30 — viz `proxmox/zalohy.md`.)

## Existující dokumenty zarchivované
- `_archiv/NICOTRANS_AI.md` ← `/opt/stacks/nicotrans-palety/NICOTRANS_AI.md`
- `_archiv/BETTER_AUTH_PROXY.md` ← `/opt/stacks/nuart-backoffice/docs/BETTER_AUTH_PROXY.md`
- `_archiv/knowledge-dump-2026-06-30.md` ← původní extrakce
- `HOMELAB.md` a `networks.md` z dumpu **na serveru nenalezeny** (neexistovaly k archivaci).

## Kontrola secrets
`git grep` na `password|secret|token|BEGIN ... PRIVATE KEY` před commitem — viz výstup v PR/commitu.
Žádné hodnoty secrets v repu; jen umístění a názvy proměnných.
